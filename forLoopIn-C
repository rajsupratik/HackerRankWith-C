#include <stdio.h>

int main() {
    int start, end;
    scanf("%d", &start);
    scanf("%d", &end);

    char* numbers[] = {"zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};

    for (int i = start; i <= end; i++) {
        if (i >= 1 && i <= 9) {
            printf("%s\n", numbers[i]);
        } else if (i % 2 == 0) {
            printf("even\n");
        } else {
            printf("odd\n");
        }
    }

    return 0;
}
